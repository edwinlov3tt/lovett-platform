# ---- dependencies + tooling caches ----
node_modules/
.pnpm-store/
.turbo/
dist/
coverage/

# Rendered HTML previews from `pnpm preview:auth`. Regenerable output;
# the source renderers live in packages/services/auth-gateway/src/pages/.
.previews/

# ---- Cloudflare / Wrangler local state ----
.wrangler/
.dev.vars
.dev.vars.*

# ---- editor / OS ----
.DS_Store
.vscode/
.idea/
*.log
npm-debug.log*

# ---- env ----
.env
.env.local
.env.*.local

# ---- secrets source-of-truth files (Worker secrets are write-only on CF,
# so keys meant for `wrangler secret put ... < file` are gitignored here)
.account-keys.json
.secrets.local
